---
phase: 07-strip-primary-analysis-and-idempotent-bond-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/opencode_service.rs
  - src/services/mod.rs
  - src/services/bonds_crawler.rs
  - Cargo.toml
autonomous: true
requirements: [REQ-009]

must_haves:
  truths:
    - "Краулер собирается (cargo build) без файла opencode_service.rs и без вызовов analyze_bond"
    - "Каждая распарсенная облигация записывается с analysis = None (краулер не пишет анализ)"
    - "В асинхронном пути парсинга больше нет блокирующего std::process::Command вызова"
  artifacts:
    - path: "src/services/mod.rs"
      provides: "Регистрация сервисов без opencode_service"
      contains: "pub mod bonds_crawler"
    - path: "src/services/bonds_crawler.rs"
      provides: "Чистый scrape+parse path без анализа и skip-логики"
  key_links:
    - from: "src/services/bonds_crawler.rs"
      to: "BondListItem"
      via: "конструирование bond_result с analysis: None"
      pattern: "analysis: None"
---

<objective>
Удалить интеграцию первичного AI-анализа (opencode) из краулера целиком. Краулер
становится чистым scrape + parse + persist. Колонка `analysis` остаётся в схеме БД
(её будет заполнять downstream obligation-api), но краулер перестаёт её писать —
значение остаётся NULL.

Purpose: Ответственность за первичный анализ переходит к obligation-api (отдельная
фаза в том репозитории). Попутно убирается блокирующий synchronous-subprocess
вызов в async-контексте (известный долг из CONCERNS.md).
Output: Краулер без opencode-кода, собирающийся и проходящий прогон без анализа.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kirilldontsov/Documents/my/obligation-crawler/.planning/STATE.md
@/Users/kirilldontsov/Documents/my/obligation-crawler/.planning/ROADMAP.md
@/Users/kirilldontsov/Documents/my/obligation-crawler/.planning/phases/07-strip-primary-analysis-and-idempotent-bond-refresh/07-CONTEXT.md
@/Users/kirilldontsov/Documents/my/obligation-crawler/.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить opencode_service и его вызов из пути парсинга</name>
  <files>src/services/opencode_service.rs, src/services/mod.rs, src/services/bonds_crawler.rs</files>
  <read_first>
    - src/services/opencode_service.rs (файл удаляется целиком)
    - src/services/mod.rs (содержит `pub mod opencode_service;` — строка 2)
    - src/services/bonds_crawler.rs (импорт ~строка 5, конструирование bond_result ~151-173, skip-analysis + вызов analyze_bond ~175-216)
    - .planning/codebase/CONVENTIONS.md (no unwrap, log макросы, Result<T> alias)
  </read_first>
  <action>
    Удалить файл src/services/opencode_service.rs целиком. В src/services/mod.rs
    удалить строку `pub mod opencode_service;`. В src/services/bonds_crawler.rs:
    удалить import `use crate::services::opencode_service::analyze_bond;`; удалить
    весь блок skip-analysis логики (вычисление `skip_analysis` по сроку погашения
    < 1 года и цене price - 1000.0 > 5.0) и последующий `if skip_analysis { ... }
    else { match analyze_bond(...) { ... } }` блок (~строки 175-216). Поле
    `analysis` в конструкторе `bond_result` (BondListItem) оставить как
    `analysis: None` — оно остаётся в struct BondListItem (REQ-009: колонка
    сохраняется, краулер пишет NULL). Не трогать запись в CSV (`append_to_csv`) и
    save в БД — они идут после удаляемого блока и должны сохраниться. Убедиться,
    что после удаления нет неиспользуемого импорта `chrono::NaiveDate` /
    `chrono::Duration`, если они использовались ТОЛЬКО в skip-логике — удалить
    осиротевшие use. Сохранить hard-tabs форматирование.
  </action>
  <verify>
    <automated>cd /Users/kirilldontsov/Documents/my/obligation-crawler && test ! -f src/services/opencode_service.rs && ! grep -rn "opencode\|analyze_bond\|skip_analysis" src/ && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл src/services/opencode_service.rs отсутствует на диске.
    - grep по src/ не находит ни `opencode`, ни `analyze_bond`, ни `skip_analysis`.
    - src/services/mod.rs не содержит `pub mod opencode_service;`.
    - bonds_crawler.rs всё ещё строит bond_result с `analysis: None` и сохраняет
      bond в CSV и БД после парсинга.
    - `cargo build` завершается успешно (без ошибок компиляции).
  </acceptance_criteria>
  <done>opencode-интеграция полностью удалена; краулер собирается; analysis всегда None.</done>
</task>

<task type="auto">
  <name>Task 2: Убрать неиспользуемые зависимости от анализа из Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (секция [dependencies]; `reqwest = "0.12.2"` ~строка 20)
    - .planning/codebase/CONCERNS.md (раздел "Dependencies at Risk": reqwest/regex/urlencoding помечены как возможно неиспользуемые)
  </read_first>
  <action>
    После удаления opencode-кода проверить, остались ли в src/ использования
    `reqwest` (grep). opencode_service использовал `std::process::Command` (stdlib,
    не крейт — удалять из Cargo.toml нечего). Если grep по src/ не находит
    `reqwest::` и нет других потребителей — НЕ удалять `reqwest` в рамках этого
    плана, только зафиксировать факт в SUMMARY (удаление неиспользуемых deps вне
    scope этой фазы по CONTEXT.md → Deferred). Если же `CrawlerError::RequestError`
    (`#[from] reqwest::Error`) в src/error.rs ссылается на reqwest — оставить reqwest
    (он нужен для компиляции error.rs). Итог этого таска: Cargo.toml не меняется
    либо меняется минимально и только если компиляция этого требует. Цель — не
    оставить orphaned dependency, ОДНОВРЕМЕННО не выходя за scope (полная зачистка
    deps отложена).
  </action>
  <verify>
    <automated>cd /Users/kirilldontsov/Documents/my/obligation-crawler && cargo build 2>&1 | tail -3 && grep -c "reqwest" src/error.rs || true</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` успешен.
    - Если reqwest остаётся в Cargo.toml — в src/ либо src/error.rs есть его
      использование (подтверждено grep), иначе он не нужен и зафиксирован как
      deferred-cleanup в SUMMARY.
    - Никаких новых зависимостей не добавлено.
  </acceptance_criteria>
  <done>Зависимости согласованы с удалённым кодом; сборка зелёная; решение по reqwest задокументировано.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped web content → crawler | Недоверенный ввод (текст со страницы T-Bank) попадал в prompt opencode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | prompt-injection через scraped name/ticker в opencode prompt | mitigate | Удаляется целиком вместе с opencode_service.rs — вектор устранён (CONCERNS.md "AI prompt built via string interpolation") |
| T-07-02 | Denial of Service | блокирующий subprocess в async-контексте (CONCERNS.md performance) | mitigate | Удаляется вместе с вызовом analyze_bond — Tokio worker больше не блокируется |
</threat_model>

<verification>
- `cargo build` успешен; `cargo clippy -- -D warnings` без новых предупреждений
  (после удаления могут исчезнуть, не добавиться).
- grep по src/ не находит opencode/analyze_bond/skip_analysis.
- Поле analysis в BondListItem остаётся, всегда конструируется как None.
</verification>

<success_criteria>
- В краулере не осталось вызовов opencode (REQ-009 acceptance).
- Краулер собирается без анализа.
- Поле analysis в записях = NULL (не заполняется).
</success_criteria>

<output>
Create `.planning/phases/07-strip-primary-analysis-and-idempotent-bond-refresh/07-01-SUMMARY.md` when done
</output>
